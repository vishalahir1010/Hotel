// firebase.js - Firebase is not used in this project
// localStorage-based auth is used instead
// This file exists only to prevent import errors from old references

export const auth = null;
export const db = null;
export default null;
